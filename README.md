# Developer instructions

Conan install:

`conan install .`

cMake setup:

`cmake --preset conan-release`

cMake build:

`cmake --build build/Release`

Run the build file:

`./build/Release/bin/main`
